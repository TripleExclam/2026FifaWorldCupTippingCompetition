# Copy this into your cloudflared tunnel config and replace the tunnel,
# credentials-file, and hostname values.
tunnel: your-tunnel-name-or-id
credentials-file: /etc/cloudflared/your-tunnel-id.json

ingress:
  - hostname: tipping.example.com
    service: http://127.0.0.1:8000
  - service: http_status:404
